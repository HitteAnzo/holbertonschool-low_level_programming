#include <stdio.h>
#include "main.h"

/**
 * _strlen _ return the length of string.
 * @s: string
 * Return: lenght
 */

int _strlen(char *s)
{
	int lenght;

	for (lenght = 0; s[lenght] != 0; lenght++)
	{
	}
	return (lenght);
}
