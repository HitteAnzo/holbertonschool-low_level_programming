#include "main.h"

/**
 * _abs - computes the abs val of an int
 * @n: is the int to compute the absolute value of
 * Return: the absolute value of the integer
 */
int _abs(int n)
{
	if (n < 0)
	{
		return (-n);
	}
	else
	{
	return (n);
	}
}
